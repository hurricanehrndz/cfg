# Sets sane Zsh built-in environment options.
zmodule environment
# Applies correct bindkeys for input events.
zmodule input
# Utility aliases and functions. Adds colour to ls, grep and less.
zmodule utility

# Performance tuning
zmodule mroth/evalcache
zmodule romkatv/zsh-defer

# Aliases and better defaults for exa
zmodule exa
# Configures fzf for fuzzy command-line history and file search.
zmodule fzf
# Git convenience with fzf integration.
zmodule wfxr/forgit

# Additional completion definitions for Zsh.
zmodule zsh-users/zsh-completions --fpath src
# Enables and configures smart and extensive tab completion.
zmodule completion

# Replace zsh's default completion selection menu with fzf.
# Must be last plugin to bind to TAB
zmodule Aloxaf/fzf-tab --cmd 'zsh-defer source ${0}'

# Starship prompt
zmodule joke/zim-starship

#
# Modules that must be initialized last
#
zmodule zsh-users/zsh-syntax-highlighting --cmd 'zsh-defer source ${0}'
zmodule zsh-users/zsh-autosuggestions --cmd 'zsh-defer source ${0}'
